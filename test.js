const { app, BrowserWindow } = require('electron'); app.whenReady().then(() => { let win = new BrowserWindow({ width: 400, height: 400 }); win.setAlwaysOnTop(true, 'screen-saver'); setTimeout(() => app.quit(), 5000); });
